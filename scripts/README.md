# Project scripts

This directory is reserved for documented seed, simulation, and maintenance commands
when their implementation sprint begins. Sprint 0 introduces no script that could
imply working database reset or simulation behavior.

General development and validation commands are defined in `package.json`.
